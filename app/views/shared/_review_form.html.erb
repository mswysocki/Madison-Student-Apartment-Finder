<%= form_for([object, object.reviews.build]) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :review_body %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>