<section class="New_Landlord" >
	<h1 class="page_header">New Landlord</h1>
	<hr class="generic_header_ruler"/>
	<br />
	<%= form_for(@landlord) do |f| %>
  		<%= render 'shared/error_messages', :object => f.object %>
  		<div class="field">
    		<%= f.label :name %><br />
    		<%= f.text_field :name %>
  		</div>
  		<div class="field">
  			<%= f.label :address %><br />
  			<%= f.text_field :address %>
  		</div>
  		<div class="field">
    		<%= f.label :email %><br />
    		<%= f.text_field :email %>
  		</div>
  		<div class="field">
    		<%= f.label :website %><br />
    		<%= f.text_field :website %>
  		</div>
  		<div class="field">
    		<%= f.label :phone %><br />
    		<%= f.text_field :phone %>
  		</div>
  		<div class="field">
  			<%= f.label :city %><br />
  			<%= f.text_field :city %>	
  		</div>
  		<div class="field">
    		<%= f.label :state %><br />
    		<%= f.text_field :state, :disabled => :true %>
  		</div>
  		<div class="field">
  			<%= f.label :zip %><br />
  			<%= f.text_field :zip %>	
  		</div>
  		<br />
  		
  		<div class="actions">
    		<%= f.submit %>
  		</div>
	<% end %>
	<br />
	Ideally, here we have a group of check box that allows them to choose
  		listings that are associated with this landlord...but I'm not sure 
  		how that would work/don't have the time yet,
  		so it may have to wait for another day.
</section>
