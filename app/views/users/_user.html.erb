<tr>  
	<td><%= user.name %> </td>
	<td><%= link_to "Show", user, :class => 'btn btn-mini' %></td>
	<td style="vertical-align: center"><% if current_user.admin? %> 
			<%= button_to "Delete", user_path(user), :method => :delete, :confirm => "You sure?",  :class => 'btn btn-mini btn-danger' %>
		<% end %>
	</td>
	
</tr>
