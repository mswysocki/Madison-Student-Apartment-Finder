<!DOCTYPE html>
<html>
<head>
  <title><%= full_title %></title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

<!-- Banner -->
<%= render 'home/banner' %>
<!-- End Banner -->

<%= yield %>


<% flash.each do |key, value| %>
	<div class="flash <%= key %>"><%= value %></div>
<% end %>

  <% if Rails.env.development? %>
<footer>
  <h3>Below are the params.  They may help with debugging. They are only going to show up in the development environment.</h3>
  <%= debug(params) if Rails.env.development? %>
</footer>
  <% end %>


</body>
</html>
